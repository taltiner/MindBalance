package com.tolgay.mindbalance.repository;

import com.tolgay.mindbalance.model.DailyFactors;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DailyFactorsRepository extends JpaRepository<DailyFactors, Long> {
}
